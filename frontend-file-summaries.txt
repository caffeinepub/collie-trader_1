{
  "frontend/src/services/binance/binanceAccountService.ts": "Binance authenticated API service for account, position risk, and order management. Fixed type error by using explicit `Record<string, string | number | boolean>` for params instead of conditional object spread.",
  "frontend/src/components/insights/TrendForecastCard.tsx": "Displays trend continuation vs reversal probability bars with confidence badge and key factors list for a given symbol/modality.",
  "frontend/src/components/risk/PositionSizeCalculator.tsx": "Interactive position size calculator with balance, risk percentage slider, entry/stop loss inputs, and computed results in contracts and USDT.",
  "frontend/src/components/risk/PortfolioExposure.tsx": "Shows per-modality capital at risk as percentage of total capital with progress bars and total exposure summary.",
  "frontend/src/components/risk/LeverageBracketTable.tsx": "Fetches and displays Binance leverage bracket data for a selected symbol in a scrollable table.",
  "frontend/src/components/risk/TradeSimulator.tsx": "Hypothetical trade simulator that calculates P&L at each TP/SL level and R:R ratio without placing real orders.",
  "frontend/src/components/recovery/RecoveryPositionCard.tsx": "Card for a deeply negative position showing loss details, expandable recovery strategies with execution buttons.",
  "frontend/src/components/recovery/RecoveryProgressTracker.tsx": "Tracks and visualizes P&L improvement after recovery actions with before/after comparison and action history.",
  "frontend/src/components/statistics/StatCard.tsx": "Reusable metric display card with label, value, optional sub-value, and color-coded positive/negative/neutral states.",
  "frontend/src/pages/Dashboard.tsx": "Dashboard updated to manage per-modality `scanningStates` map, passing `isScanning` to each `ModalityCard`, and using wrapper handlers (`handleGenerate`, `handleInitializeAll`) that set/clear scanning state around async trade generation calls.",
  "frontend/src/pages/AIInsights.tsx": "AI analysis page showing sentiment, trend forecast, and trade advisor suggestions per active trade modality.",
  "frontend/src/pages/RiskManagement.tsx": "Risk management page composing position size calculator, portfolio exposure, leverage bracket table, and trade simulator.",
  "frontend/src/pages/PositionRecovery.tsx": "Recovery tab that auto-detects positions with >20% loss and offers strategy execution with progress tracking.",
  "frontend/src/pages/Statistics.tsx": "Performance statistics page showing overall and per-modality metrics (win rate, R:R, P&L, TP/SL hits) from localStorage trade history.",
  "frontend/src/pages/Settings.tsx": "Settings page with API credentials form, CORS proxy warning banner, live trading toggle, and capital settings — updated test result display to show a dedicated balance row (with Wallet icon and USDT amount) on success, and a contextual tip about corsproxy.io availability on network-error failures.",
  "frontend/src/App.tsx": "Root application component with TanStack Router setup for all 6 routes, AppLayout wrapper with Outlet, ThemeProvider (dark), and Sonner toaster.",
  "frontend/src/services/ai/pairScoringEngine.ts": "New multi-factor pair scoring engine implementing the 8-module knowledge framework (Volume/Capital Flow, Price Action/SMC, Candlestick patterns, Chart patterns, Market psychology, Strategy alignment, Risk management). Exports `scoreSymbolsForModality(modality, symbols, limit)` which fetches klines for each symbol, computes a composite 0–100 score, and returns top candidates with human-readable `scoringFactors`.",
  "frontend/src/services/ai/aiTradeSelection.ts": "Refactored AI trade selection service where `generateTradeSetup(modality)` no longer accepts a symbol argument — it autonomously fetches all USDⓈ-M Perpetual symbols, scores them via `pairScoringEngine.ts`, and iterates through the top 10 candidates to find the first valid trade setup with proper SMC signals and 1:2 R:R.",
  "frontend/src/types/trade.ts": "Trade type definitions with `scoringFactors?: string[]` added to both `Trade` and `TradeSetup` interfaces to persist the AI pair selection rationale alongside active and closed trades.",
  "frontend/src/hooks/useTradeLifecycle.ts": "Trade lifecycle hook updated to call `generateTradeSetup(modality)` without a symbol argument (AI selects autonomously), and persists `scoringFactors` from the setup into the active trade object stored in localStorage.",
  "frontend/src/components/trading/ModalityCard.tsx": "Modality card updated with `isScanning` prop that shows an animated \"Scanning market...\" indicator with bouncing dots and descriptive text while the AI selects a pair; active trades now display an Info icon button that toggles an expandable \"Why this pair?\" rationale section listing the AI's top scoring factors.",
  "frontend/src/services/binance/corsConfig.ts": "CORS proxy configuration exporting `CORS_PROXY_URL = 'https://corsproxy.io/?url='` — updated to use the correct `?url=` query parameter format required by corsproxy.io (the old `?` prefix without `url=` no longer works).",
  "frontend/src/services/binance/binanceAuth.ts": "Updated authenticated and public fetch functions to route all Binance API calls through the CORS proxy via a `proxify()` helper that prepends `CORS_PROXY_URL` to the full URL; HMAC-SHA256 signature generation remains entirely client-side before proxying.",
  "frontend/src/services/binance/binancePublicApi.ts": "Public Binance Futures API service (exchange info, ticker prices, klines, leverage brackets) — now implicitly proxied through corsproxy.io via the updated `publicFetch` in `binanceAuth.ts`; added a top-level comment marking this as a temporary CORS workaround.",
  "frontend/src/hooks/useCredentials.ts": "Credentials hook managing API key/secret state, save, testConnection (calls `getAccount()` via the proxied `authenticatedFetch`), and live trading toggle — completely rewritten `parseErrorMessage` helper that distinguishes timeout, proxy unreachable, invalid API key, invalid signature, IP whitelist, permissions, rate limit, and server errors with clear Portuguese/English user-friendly messages. The `testResult` type now includes an optional `balance` field returned on success."
}